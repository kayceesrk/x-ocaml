(* OCamlformat is disabled on the OxCaml branch: ocamlformat-lib 0.26.2+ox
   bundles its own copy of [Ocaml_common] which clashes with the one pulled
   in via merlin-lib's compiler-libs in this worker. *)
let fmt source = source
